const { width, height, availWidth, availHeight } = window.screen
const { innerHeight, innerWidth } = window

export {
  width as Width,
  height as Height,
  innerWidth as InnerWidth,
  availWidth as AvailWidth,
  innerHeight as InnerHeight,
  availHeight as AvailHeight,
}
